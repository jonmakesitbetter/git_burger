    <li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevour="{{devour}}">
		{{#if devour}}BURGER TIME!{{else}}EAT UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>